# ========
# Enable or disable username
# Maintainer: mcrozes@
# ========

- hosts: localhost
  tasks:
    - name: Configure specific shell for a given POSIX user
      user:
        name: {{ user }}
        shell: {{ shell }}


